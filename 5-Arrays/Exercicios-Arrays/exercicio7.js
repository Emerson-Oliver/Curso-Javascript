let meuArray = [];

meuArray.push ("Maça");

meuArray.push(1, 2, 3);

let outroArray = [4, 5, 6];

let arrayConcatenado = meuArray.concat(outroArray);

arrayConcatenado.pop();

console.log(arrayConcatenado);
